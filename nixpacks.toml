providers = ["php"]

[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.gd", "php82Extensions.zip", "php82Extensions.pdo_mysql", "php82Extensions.mbstring"]

[phases.build]
cmds = ["composer install --optimize-autoloader --no-dev"]